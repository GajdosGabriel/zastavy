
import { formatDecimal } from './functions'
import useOrder from '../store/StoreOrders';


export default function templateCustomer(state) {

  // state.price_sum = formatDecimal(data.price_sum)

  // state.button = { name: 'Uložiť', spinner: false }

  // state.buttonSubmit = buttonSubmit
  // state.buttonTopRight = buttonTopRight
  // state.buttonBottomLeft = buttonBottomLeft
  // state.buttonLinkHeader = { name: 'Späť', spinner: true, link: 'customers.index', icon: 'arrow-left' }

  return state;
}

// // const buttonTopRight = { name: 'Nový zákazník', spinner: true, link: 'customers.create', icon: 'plus' }
// const buttonSubmit = { name: 'Uložiť', spinner: true }
// const buttonBottomLeft = { name: 'Späť', spinner: true, link: 'customers.index', icon: 'arrow-left' }

